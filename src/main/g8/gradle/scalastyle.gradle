apply plugin: 'scalaStyle'

scalaStyle {
  configLocation = $rootDir+"/gradle/conf/scalastyle_config.xml"
  includeTestSourceDirectory = true
  source = "src/main/scala"
  testSource = "src/test/scala"
}

